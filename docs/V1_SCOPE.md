# V1 scope

V1 provides local SAF selection, recursive `.sdocx` folder discovery, shared-file intake, bounded SDOCX ZIP parsing, Markdown/front matter, original/media attachments, handwriting SVGs, reports, and a real CreateDocument ZIP export. Locked notes are reported and skipped. OCR, decryption, cloud APIs, and background sync are non-goals.
